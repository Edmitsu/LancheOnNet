{
  "dependencies": {
    "concurrently": "^8.2.1"
  },
  "scripts": {
    "start": "concurrently \"npm run backend\" \"npm run frontend\" \"npm run pagamento\"",
    "backend": "cd Src && npm start", 
    "frontend": "cd Public && npm run dev",
    "pagamento": "cd Pagamento && nodemon app.js",
    "install-all": "cd Src && npm install && cd ../Public && npm install && cd ../Pagamento && npm install" 
  }
  
  
}
